specVersion: "v1beta"
serviceId: "backend"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
